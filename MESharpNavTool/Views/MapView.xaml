<UserControl x:Class="MESharp.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:MESharp.Converters"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{DynamicResource App.CardBrush}" CornerRadius="6" Padding="8,4" Margin="0,0,0,6">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Click="OnHelpClick" Width="26" Padding="0,3" Margin="0,0,6,0"
                            ToolTip="What can I do on this map? Modes, recording, layers and live-session actions explained.">
                        <TextBlock Text="?" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center" />
                    </Button>
                    <Button Click="OnReloadClick" Padding="8,3" Margin="0,0,6,0" ToolTip="Reload the map page (picks up graph/route edits)">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14" Margin="0,0,5,0" VerticalAlignment="Center"
                                                        Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                            <TextBlock Text="Reload" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenInBrowserCommand}" Padding="8,3" ToolTip="Open the same live map in your default browser">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="OpenInNew" Width="14" Height="14" Margin="0,0,5,0" VerticalAlignment="Center"
                                                        Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                            <TextBlock Text="Browser" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <TextBlock Text="{Binding Status}" VerticalAlignment="Center"
                           Foreground="{DynamicResource App.SubtleForegroundBrush}" FontSize="11" TextTrimming="CharacterEllipsis" />
            </DockPanel>
        </Border>

        <!-- WebView2 host (control is created in code-behind so init failures degrade gracefully) -->
        <Border Grid.Row="1" x:Name="WebHostBorder"
                Background="{DynamicResource App.CardBrush}" CornerRadius="6" ClipToBounds="True">
            <Grid x:Name="WebHost" />
        </Border>

        <!-- Fallback when WebView2 is unavailable -->
        <Border Grid.Row="1" Background="{DynamicResource App.CardBrush}" CornerRadius="6" Padding="24"
                Visibility="{Binding WebViewFailed, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="520">
                <iconPacks:PackIconMaterial Kind="WebOff" Width="40" Height="40" HorizontalAlignment="Center"
                                            Foreground="{DynamicResource App.MutedBrush}" Margin="0,0,0,12" />
                <TextBlock Text="The embedded map could not start." FontSize="15" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Foreground="{DynamicResource App.ForegroundBrush}" />
                <TextBlock Text="{Binding WebViewError}" Margin="0,8,0,12" TextWrapping="Wrap"
                           Foreground="{DynamicResource App.SubtleForegroundBrush}" FontSize="12" />
                <TextBlock Text="Install the Microsoft Edge WebView2 Runtime (Evergreen), or use the external browser instead — same live map, same tools."
                           TextWrapping="Wrap" Foreground="{DynamicResource App.SubtleForegroundBrush}" FontSize="12" Margin="0,0,0,12" />
                <Button Command="{Binding OpenInBrowserCommand}" HorizontalAlignment="Center" Padding="14,6" Content="Open Map in Browser" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
